/**
 * Flat solid background — no gradients, no moving glows.
 * The body background-color (#0B0F0D carbon) carries the surface; this
 * component is intentionally empty so the palette stays clean and sober.
 */
export function AnimatedBackground() {
  return null;
}
